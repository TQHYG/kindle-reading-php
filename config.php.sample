<?php
// config.php - 全局配置文件

return [
    // 1. 数据库配置
    'db' => [
        'host'     => '127.0.0.1',
        'dbname'   => 'kykky_stats', // 你的数据库名
        'user'     => 'root',        // 数据库用户名
        'password' => 'your_password', // 数据库密码
        'charset'  => 'utf8mb4',
    ],

    // 2. OAuth2 配置 
    'oauth' => [
        'provider'      => 'github',
        'url_authorize'    => 'https://github.com/login/oauth/authorize',
        'url_access_token' => 'https://github.com/login/oauth/access_token',
        'url_user_info'    => 'https://api.github.com/user',
        
        'client_id'     => '你的_CLIENT_ID',
        'client_secret' => '你的_CLIENT_SECRET',
        // 回调地址，手机扫码后跳转回这里处理登录
        'redirect_uri'  => 'https://你的域名.com/callback.php', 
    ],

    // 3. 文件存储路径
    'storage' => [
        // 确保这个目录存在，并且 PHP 有写入权限 (chmod 755 或 777)
        'base_path' => __DIR__ . '/storage/data/', 
    ],

    // 4. 管理员配置
    'admin' => [
        // 在这里填入管理员的 user_id (对应 users 表的 id)
        // 比如你的 id 是 1，就填 [1]
        'ids' => [1], 
    ],
    
    // 5. JWT 或 Token 密钥 (用于生成发给 Kindle 的 Access Token)
    'security' => [
        'jwt_secret' => '请修改为一段很长很复杂的随机字符串',
    ]
];